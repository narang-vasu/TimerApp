import { StyleSheet } from "react-native";

export const SplashStyle = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#fff',
        justifyContent: 'center',
        alignItems: 'center'
    },
    text1: {
        fontSize: 20,
        fontWeight: '600',
        color: '#000'
    }
})